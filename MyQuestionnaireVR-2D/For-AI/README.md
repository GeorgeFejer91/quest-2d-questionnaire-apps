# For AI Agents Working On MyQuestionnaireVR-2D

Every agent working on `MyQuestionnaireVR-2D` must first read the repository
AI hub:

```text
../../For-AI/START_HERE.md
```

Then read this folder before changing app-specific code.

Start with:

1. `project-goals.md`
2. `questionnaire-builder-gui-constraints.md`

Also read the builder publication handoff when changing the GUI or Pages copy:

```text
../../For-AI/published-questionnaire-builder.md
```

Also read the handoff lessons when changing ChainLink, Unity bridge helpers,
questionnaire launch extras, or Quest validation:

```text
../../For-AI/handoff-implementation-lessons.md
```

Keep this folder current when the user adds architectural decisions,
constraints, or terminology that future agents must preserve.
